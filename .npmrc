# Enforce exact version pinning — no carets or tildes
save-exact=true
# Strict peer deps resolution
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
prefer-frozen-lockfile=true
