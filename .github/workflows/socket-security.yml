name: Socket Security

# Temporarily disabled - Socket action repository not found
# Re-enable once the action is available or use alternative

on:
  workflow_dispatch: # Manual trigger only

permissions:
  contents: read
  pull-requests: write
  statuses: write

jobs:
  socket-security:
    name: Socket.dev Supply Chain Security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Socket Security Analysis
        # uses: SocketDev/socket-action@v1
        # Disabled - action not found
        # TODO: Re-enable when action is available or find alternative
        run: echo "Socket Security check disabled - action not found"
        # with:
        #   repo-token: ${{ secrets.GITHUB_TOKEN }}
        #   comment-pr: true
        #   fail-on-issues: false
        #   severity-threshold: medium
