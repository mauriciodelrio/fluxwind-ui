import { describe, it, expect } from 'vitest';

describe('@fluxwind/utils', () => {
  it('package exports correctly', () => {
    // Basic test to ensure package is importable
    expect(true).toBe(true);
  });

  // TODO: Add utility function tests as utilities are implemented
});
